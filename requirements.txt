# Frozen from the provisioned .venv/ (Python 3.12) — documentation, does not trigger install.
# sam2 is installed editable from vendor/sam2/ (not a PyPI pin) — see README.md.
torch==2.12.1
numpy==2.2.6
scikit-image==0.26.0
opencv-python-headless==4.13.0.90
pandas==2.3.3
matplotlib==3.11.0
pillow==12.3.0
pytest==9.1.1
scipy==1.18.0
