---
phase: 01-walking-skeleton-single-thread-click-to-csv
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - src/segment/click_loop.py
  - src/segment/segment_export.py
  - run_pipeline.py
  - tests/test_skeleton_e2e.py
  - tests/test_click_loop.py
  - MORNING-TEST.md
autonomous: true
requirements: [SEG-01, SEG-02]
must_haves:
  truths:
    - "The interactive click loop is wired: left-click=positive, right-click=negative, keys accept/erase, calling predict_mask + export_mask + erase_region"
    - "Stage-1 CLI globs a photo folder and drives the click loop per photo, prompting for thread number"
    - "run_pipeline drives segment(programmatic click)->export->measure->calibrate->build_csv end-to-end and produces a final.csv row on real data"
    - "The final.csv row's AvgDiameter(mm) is within a plausible range vs the 4 ImageJ ground-truth values"
  artifacts:
    - path: "src/segment/click_loop.py"
      provides: "interactive matplotlib click UI (D-01) wiring predict_mask/erase_region/export_mask"
      exports: ["run_click_loop"]
      min_lines: 60
    - path: "src/segment/segment_export.py"
      provides: "Stage-1 CLI: glob folder, per-photo metadata+thread prompt, launch click loop"
      exports: ["main"]
      min_lines: 30
    - path: "run_pipeline.py"
      provides: "non-interactive end-to-end orchestrator proving the walking skeleton"
      exports: ["run"]
      min_lines: 40
    - path: "tests/test_skeleton_e2e.py"
      provides: "slow integration test: real photo + programmatic click -> final.csv with exact header + plausible mm"
    - path: "MORNING-TEST.md"
      provides: "step-by-step Mac validation checklist (MPS, real clicking, erase UX, calibration zoom)"
  key_links:
    - from: "run_pipeline.py"
      to: "segment.sam2_session + measure + calibrate + join"
      via: "orchestrates all stages non-interactively"
      pattern: "predict_mask|measure_folder|build_final_csv"
    - from: "src/segment/click_loop.py"
      to: "src/segment/sam2_session.py + export.py + mask_edit.py"
      via: "click callbacks call predict_mask, erase_region, export_mask"
      pattern: "predict_mask|erase_region|export_mask"
---

<objective>
Complete the walking skeleton: the interactive click loop that a human actually drives (D-01, SEG-01,
SEG-02), the Stage-1 CLI that runs it over a photo folder (D-05), the non-interactive `run_pipeline.py`
orchestrator that proves the full stack end-to-end on real data, the `slow` integration test that
asserts a correct `final.csv` row, and `MORNING-TEST.md` — the handoff doc telling the user exactly what
to validate by hand on the M2 Mac tomorrow (MPS backend, real clicking, erase UX, calibration zoom).

**Phase user story (met by this plan):** As a thread-compaction researcher, I want to click once to
segment a thread photo, correct the mask if SAM2 grabs the needle, and run the pipeline against a ruler
photo, so that I get a correct CSV row in the exact column format my existing R script consumes.

Purpose: This plan turns four independently-tested stages into one proven pipeline. The click loop is the
ONE component that cannot be interactively verified in tonight's headless unattended session, so it is
built to the RESEARCH Pattern 2 reference shape and verified only structurally (imports under the Agg
backend, callbacks correctly wired to the already-tested engine functions); its click UX is validated on
the Mac per the morning doc. `run_pipeline.py` supplies the click programmatically, so the walking
skeleton's end-to-end path IS exercised and asserted tonight, GUI aside. Running "for real" against more
than one thread is the same code path per D-06 — batch-scale trust work (idempotency, ImageJ validation,
hard-fail nets, manifest) stays deferred to Phase 2.

Output: `click_loop.py`, `segment_export.py`, a real `run_pipeline.py`, the end-to-end integration test,
and `MORNING-TEST.md`. After this plan the phase goal is met.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-single-thread-click-to-csv/SKELETON.md
@.planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-CONTEXT.md
@src/segment/sam2_session.py
@src/segment/export.py
@src/segment/mask_edit.py
@src/measure/measure_masks.py
@src/calibrate/ruler_scale.py
@src/join/build_final_csv.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: click_loop.py — interactive matplotlib click UI (D-01) + Stage-1 CLI</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-RESEARCH.md (Pattern 2 click-loop shape: mpl_connect button_press_event, left=positive/right=negative, draw_idle, key=accept then export; reset-click-state-per-photo pitfall)
    - .planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-CONTEXT.md (D-01 bespoke loop, D-08 negative-point-then-erase, D-07 thread number prompted at click time)
    - src/segment/sam2_session.py (predict_mask), src/segment/export.py (export_mask), src/segment/mask_edit.py (erase_region), src/segment/naming.py (parse + canonical_stem)
    - .planning/research/PITFALLS.md (Pitfall 2 click-state contamination across photos; warmup UX)
    - .claude/skills/matplotlib/SKILL.md (mpl_connect, event.button/xdata, draw_idle, key_press_event)
  </read_first>
  <files>src/segment/click_loop.py, src/segment/segment_export.py, tests/test_click_loop.py</files>
  <action>
    Implement `run_click_loop(predictor, image_rgb, on_accept)` per RESEARCH Pattern 2: maintain
    `positive_points`/`negative_points` lists, a `button_press_event` callback (left=1 append positive,
    right=3 append negative) that recomputes the mask via `predict_mask` and redraws with `draw_idle`, a
    `key_press_event` callback where `a` accepts (calls `on_accept(mask)` which does export), `e` toggles
    a raster-erase drag mode that applies `erase_region` to the accepted mask, and `n` skips to the next
    photo. CRITICAL: clear `positive_points`/`negative_points`/accepted-mask state at the start of each
    photo (RESEARCH Pitfall 2 — stale clicks contaminate the next image). Implement `segment_export.py`
    `main()` argparse (`--input-dir`, `--nextcloud-root`, optional `--date/--batch/--condition`
    overrides for flat/legacy data): glob the folder for photos, per photo derive metadata via
    `parse_photo_path` (or overrides / `parse_flat_path`), `input()`-prompt for the thread number (D-07 —
    not inferable from a nested filename), `set_image` once, then launch `run_click_loop` with an
    `on_accept` that builds the stem via `canonical_stem` and calls `export_mask`. Do NOT block the whole
    unattended run on a display — guard the `plt.show()` path so importing the module and constructing the
    loop object works under the Agg backend. Add `tests/test_click_loop.py` (normal, Agg backend, NO real
    display): inject a fake predictor whose `predict_mask` returns a canned mask, simulate two synthetic
    `button_press_event`-shaped objects into the click callback, and assert the accumulated point/label
    arrays are correct (left produces label 1, right produces label 0) and that an accept invokes the
    injected `on_accept`/export exactly once — structurally proving the wiring without a human or a window.
  </action>
  <verify>
    <automated>cd /home/bcjamal/projects/thread-compaction-analysis-auto && MPLBACKEND=Agg .venv/bin/python -m pytest tests/test_click_loop.py -v && MPLBACKEND=Agg .venv/bin/python -c "import sys; sys.path.insert(0,'src'); import segment.click_loop, segment.segment_export; print('click loop + CLI import OK under Agg')"</automated>
  </verify>
  <acceptance_criteria>
    - `test_click_loop.py` asserts a left-click appends a positive point (label 1) and a right-click a negative point (label 0), and that accept triggers the export callback exactly once.
    - Both `segment.click_loop` and `segment.segment_export` import successfully under `MPLBACKEND=Agg` (no display required).
    - Click state is reset per photo (a test or inspection confirms lists are cleared before a new image).
    - `python -m segment.segment_export --help` runs (folder-level CLI wired, D-05).
  </acceptance_criteria>
  <done>The bespoke click loop is wired to the tested engine (predict_mask/erase_region/export_mask), resets state per photo, and the Stage-1 CLI globs a folder and prompts for thread number; structural tests green. Interactive UX deferred to MORNING-TEST.md.</done>
</task>

<task type="auto">
  <name>Task 2: run_pipeline.py end-to-end orchestrator + skeleton integration test</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-single-thread-click-to-csv/SKELETON.md (capability proven end-to-end; the non-interactive stage order; schema contract)
    - .planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-CONTEXT.md (D-05 matching by date; ground-truth values 5.11=0.789mm etc. for plausibility; D-06 real multi-thread is same path)
    - src/segment/sam2_session.py, src/segment/export.py, src/measure/measure_masks.py, src/calibrate/ruler_scale.py, src/join/build_final_csv.py
    - tests/conftest.py (sample_photo_path, ruler_photo_path, ground_truth_imagej, data_root)
    - .planning/STATE.md (build box CPU-only; SAM2 slow; ground-truth folder 08-03-25)
  </read_first>
  <files>run_pipeline.py, tests/test_skeleton_e2e.py</files>
  <action>
    Replace the `run_pipeline.py` stub (from plan 01) with a real `run(photo_path, click_points,
    click_labels, ruler_path, ruler_points, known_cm_span, date, batch, condition, thread, data_root)`
    that drives the whole non-interactive path: load predictor + `predict_mask` on the photo with the
    supplied programmatic click coords (standing in for the GUI click), `export_mask` under `data_root`,
    `measure_folder` over the masks dir into `measurements.csv`, compute the calibration row from
    `px_per_cm(ruler_points, known_cm_span)` into `calibration.csv`, then `build_final_csv` into
    `final.csv`; return the final DataFrame. Add a `__main__` argparse wrapper for manual invocation. Add
    `tests/test_skeleton_e2e.py` marked `slow`+`integration` (skip if `sample_photo_path`/`ruler_photo_path`
    missing): call `run(...)` against real `08-03-25/5.11.JPG` with a hardcoded on-thread click point, the
    real `ruler.JPG` with two hardcoded tick points spanning 0.5 cm, `date="2025-08-03"`, `thread="5.11"`;
    assert the produced `final.csv` first line equals the exact 9-column R header, has exactly one data
    row, and that `AvgDiameter(mm)` is a positive value in a LOOSE plausibility band around the ImageJ
    ground truth (e.g. 0.3–3.0 mm — the 4 known values are 0.789–0.865; this is a sanity check per the
    environment note, NOT a strict pass/fail — thin ground truth). Use `data_root` (tmp) so no repo `data/`
    pollution. This test is the walking-skeleton proof: real photo + programmatic click -> correct CSV row.
  </action>
  <verify>
    <automated>cd /home/bcjamal/projects/thread-compaction-analysis-auto && MPLBACKEND=Agg .venv/bin/python -m pytest tests/test_skeleton_e2e.py -v -m slow</automated>
  </verify>
  <acceptance_criteria>
    - `run(...)` executes segment→export→measure→calibrate→build end-to-end and returns a 1-row DataFrame.
    - The slow e2e test asserts `final.csv` header equals the literal 9-column R contract in exact order (AC-008).
    - `AvgDiameter(mm)` is positive and within the loose 0.3–3.0 mm plausibility band (sanity, not strict).
    - The test writes only under a tmp `data_root`, never the repo `data/` tree, and skips cleanly if real data is absent.
  </acceptance_criteria>
  <done>run_pipeline drives the whole non-interactive pipeline and the slow integration test proves a real photo + programmatic click yields a schema-exact final.csv row with a plausible mm value.</done>
</task>

<task type="auto">
  <name>Task 3: MORNING-TEST.md — Mac hand-validation handoff doc</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-BRIEF.md (AC-001/002/005/007/009 manual verification steps)
    - .planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-CONTEXT.md (D-02 MPS validation still needed on Mac; D-10 calibration zoom; overnight-build note)
    - .planning/research/PITFALLS.md (Pitfall 1 MPS quirks to watch; warmup UX; "looks done but isn't" checklist)
    - .planning/phases/01-walking-skeleton-single-thread-click-to-csv/SKELETON.md (run commands, directory layout)
  </read_first>
  <files>MORNING-TEST.md</files>
  <action>
    Write `MORNING-TEST.md` at the repo root as the user's morning checklist for the M2 Mac (the things
    that could NOT be validated in tonight's headless CPU session). Include: (1) how to run the fast unit
    suite and the slow suite on the Mac (`pytest`, `pytest -m slow`); (2) MPS validation — confirm
    `select_device()` returns `mps`, run `segment_export.py` on a real photo and watch for the Pitfall-1
    signatures (float64 TypeError, "Placeholder storage not allocated on MPS", feathered/islanded masks vs
    CPU), with the CPU fallback as the escape hatch (SEG-03); (3) interactive click UX (AC-001/002) —
    left-click positive, right-click negative to drop the needle, `e` to raster-erase, `a` to accept/export,
    verify the exported mask covers only the thread; (4) overlay QC check (AC-005) — open a
    `data/qc/*_overlay.png` and confirm it matches the mask; (5) calibration (AC-007, D-10) — run the ruler
    step on `ruler.JPG`, zoom into the fine mm ticks, click a 0.5 cm span, sanity-check the px/cm factor by
    hand; (6) multi-file folder run (AC-009); (7) the full end-to-end command and where `final.csv` lands;
    (8) a short "known open risks" section (MPS preliminary support, curved-thread measurement bias to be
    validated against ImageJ in Phase 2 per MEAS-03). Keep it a concrete numbered checklist, not prose.
  </action>
  <verify>
    <automated>cd /home/bcjamal/projects/thread-compaction-analysis-auto && test -f MORNING-TEST.md && grep -qi "mps" MORNING-TEST.md && grep -qi "0.5" MORNING-TEST.md && echo "MORNING-TEST.md present with MPS + calibration-span steps"</automated>
  </verify>
  <human-check>
    On the M2 Mac tomorrow, work through MORNING-TEST.md end to end: MPS device, real click-to-segment on a
    real thread photo, needle correction via negative point + erase, overlay QC, ruler calibration with a
    zoomed 0.5 cm span, and confirm final.csv opens correctly for the R script. This is the deferred
    human verification for AC-001/002/005/007/009 under end-of-phase human_verify_mode.
  </human-check>
  <acceptance_criteria>
    - `MORNING-TEST.md` exists at repo root and contains numbered steps covering MPS validation, interactive click+erase, overlay QC, and the 0.5 cm calibration span (D-10).
    - It names the exact run commands and where `final.csv` is written.
    - It lists the known open risks (MPS preliminary, curved-thread measurement to be validated in Phase 2).
  </acceptance_criteria>
  <done>A concrete morning checklist exists so the user can validate every interactive/MPS-dependent AC by hand on the Mac; grep confirms the key sections.</done>
</task>

</tasks>

<artifacts_produced>
- `src/segment/click_loop.py` — `run_click_loop(predictor, image_rgb, on_accept)` interactive matplotlib UI (D-01)
- `src/segment/segment_export.py` — `main()` Stage-1 CLI (`python -m segment.segment_export`), folder glob + thread prompt + click loop
- `run_pipeline.py` — real `run(...)` non-interactive end-to-end orchestrator (replaces plan-01 stub)
- `tests/test_click_loop.py` — structural click-wiring test (Agg, no display)
- `tests/test_skeleton_e2e.py` — `slow`+`integration` real-data end-to-end proof
- `MORNING-TEST.md` — Mac hand-validation checklist (deferred human verification)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human click → segmentation | interactive UI drives model + writes; deferred to Mac validation |
| SAM2/MPS on Mac | preliminary MPS support may diverge from CPU (Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Info/Tampering | stale click state contaminating the next photo's mask | mitigate | Explicit per-photo reset of positive/negative point lists (RESEARCH Pitfall 2); noted in MORNING-TEST as a watch item |
| T-05-02 | Tampering | MPS numerical divergence producing subtly wrong masks on the Mac | transfer/mitigate | Deferred hand-validation in MORNING-TEST with CPU fallback escape hatch (SEG-03); manual correction step retained |
| T-05-03 | Tampering | e2e test polluting or writing into repo data/ or Nextcloud source | mitigate | Test writes only under tmp `data_root`; export writes under data/ only (inherited from plan 04) |
| T-05-SC | Tampering | package supply chain | accept | No new installs; `.venv/` + vendored checkpoint pre-provisioned and audited (RESEARCH) |
</threat_model>

<verification>
- Fast suite: `pytest tests/test_click_loop.py` green under Agg; both segmentation CLIs import headless.
- Slow suite: `pytest -m slow tests/test_skeleton_e2e.py` produces a schema-exact final.csv with a plausible mm value on real data.
- `MORNING-TEST.md` present with MPS + calibration-span + interactive-click sections.
- Deferred: interactive click/MPS/calibration UX validated by the user on the Mac per MORNING-TEST.md (end-of-phase human_verify_mode).
</verification>

<success_criteria>
The walking skeleton is complete and proven: interactive click loop wired to the tested engine, Stage-1
CLI over a folder, a non-interactive orchestrator, a real-data end-to-end integration test asserting the
exact R CSV contract and a plausible mm value, and a morning handoff doc for the Mac-only manual checks.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-single-thread-click-to-csv/01-05-SUMMARY.md` when done.
</output>
