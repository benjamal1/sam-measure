---
phase: 02-batch-hardening-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validate/__init__.py
  - src/validate/imagej_validation.py
  - scripts/run_imagej_validation.py
  - tests/test_imagej_validation.py
  - .planning/phases/02-batch-hardening-validation/VALIDATION.md
autonomous: true
requirements: [MEAS-03]
user_setup: []

must_haves:
  truths:
    - "Measured avg_diameter and stdev for all 4 ImageJ ground-truth threads (5.11/5.12/5.21/5.22 in 08-03-25) are reported against ImageJ values with per-thread percent difference in VALIDATION.md (MEAS-03, D2-02)"
    - "VALIDATION.md documents the StDev-vs-ImageJ overshoot as the known signature of boundary-roughness contamination (Pitfall 2) and points at MEAS-04 (v2, deferred) rather than changing the measurement method (D2-03)"
    - "The measurement algorithm (src/measure/measure_masks.py) is NOT modified by this plan — validation only (D2-03)"
  artifacts:
    - path: "src/validate/imagej_validation.py"
      provides: "Pure compare_to_imagej() + render_validation_md() — px→mm conversion, per-thread percent diff, stdev-overshoot finding"
      contains: "def compare_to_imagej"
    - path: ".planning/phases/02-batch-hardening-validation/VALIDATION.md"
      provides: "The MEAS-03 validation report: measured vs ImageJ across all 4 ground-truth threads"
      contains: "5.11"
    - path: "tests/test_imagej_validation.py"
      provides: "Unit tests for the pure comparison + a slow real-SAM2 validation run that emits VALIDATION.md"
      exports: []
  key_links:
    - from: "scripts/run_imagej_validation.py"
      to: "src/measure/measure_masks.py"
      via: "runs measure_mask() on the real SAM2 masks (reuse, no edit)"
      pattern: "measure_mask"
    - from: "scripts/run_imagej_validation.py"
      to: "src/segment/sam2_session.py"
      via: "real SAM2 CPU inference via predict_mask() for the 4 ground-truth threads"
      pattern: "predict_mask"
    - from: "scripts/run_imagej_validation.py"
      to: "src/validate/imagej_validation.py"
      via: "feeds measured px + derived px_per_cm into compare_to_imagej and writes VALIDATION.md"
      pattern: "compare_to_imagej"
---

<objective>
Validate the Phase-1 measurement method against real ImageJ ground truth, systematically across all 4 known threads (5.11/5.12/5.21/5.22 in the real 08-03-25 folder) — replacing Phase 1's single informal spot-check. Implements MEAS-03 per D2-02 (report measured vs ImageJ with percent diff in a VALIDATION.md; documented plausibility/regression check, NOT a strict pass/fail gate — only 4 ground-truth rows exist) and D2-03 (no new measurement algorithm; if the axis-sort method is unacceptably off, that is a documented finding pointing at MEAS-04, not a fix here).

Phase framing: this plan adds NO new pipeline stage and does NOT touch measure_masks.py — it is a standalone validation harness plus report. It is the heaviest plan (real SAM2 CPU inference ×4 on macro photos), which is why it is isolated. File scope does not overlap plans 01/02, so all three run in parallel (wave 1).

Purpose: before trusting the measurement on new data, prove on the shared ImageJ sample set how close it lands and where it diverges (Pitfall 2 predicts StDev overshoot from mask-boundary roughness).
Output: `src/validate/imagej_validation.py`, a real-inference harness, `VALIDATION.md`, and unit + slow tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-batch-hardening-validation/02-CONTEXT.md
@.planning/phases/01-walking-skeleton-single-thread-click-to-csv/SKELETON.md
@.planning/research/PITFALLS.md

# Existing code reused UNCHANGED (read, do not edit)
@src/measure/measure_masks.py
@src/segment/sam2_session.py
@src/calibrate/ruler_scale.py
@tests/conftest.py
@tests/test_segment_smoke.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure ImageJ comparison + report module (src/validate/imagej_validation.py)</name>
  <files>src/validate/__init__.py, src/validate/imagej_validation.py, tests/test_imagej_validation.py</files>
  <read_first>
    - tests/conftest.py (the ground_truth_imagej fixture — the exact 4-thread avg/stdev mm values + the 5.22 stdev=None case)
    - src/calibrate/ruler_scale.py (px_per_cm signature — how a factor converts px→cm; mm = px / px_per_cm * 10)
    - src/join/build_final_csv.py (the mm-conversion formula AvgDiameter(mm)=avg_px/px_per_cm*10 — mirror it exactly)
    - .planning/research/PITFALLS.md (Pitfall 2 — StDev systematically higher than ImageJ = boundary-roughness signature, the finding to surface)
    - .planning/phases/02-batch-hardening-validation/02-CONTEXT.md (D2-02 plausibility-not-gate; D2-03 point at MEAS-04, do not fix)
  </read_first>
  <behavior>
    - compare_to_imagej(measured_px, ground_truth_mm, px_per_cm) returns a report dict: per thread {avg_diameter_mm (measured), avg_diameter_mm_imagej, avg_pct_diff, stdev_mm (measured), stdev_mm_imagej|None, stdev_pct_diff|None}; and a summary {mean_abs_avg_pct_diff, stdev_overshoot (bool: measured stdev systematically > ImageJ), n_threads}.
    - mm conversion mirrors build_final_csv exactly: mm = px / px_per_cm * 10.
    - Threads whose ImageJ stdev is None (e.g. 5.22) are handled without crashing (stdev_pct_diff None, still reports the measured value).
    - render_validation_md(report, context) returns a Markdown string: a per-thread table (thread, measured mm, ImageJ mm, % diff for both avg and stdev), a summary line, an explicit Pitfall-2 note if stdev_overshoot is true, and a MEAS-04-deferred pointer (D2-03).
  </behavior>
  <action>
    Create the `validate` package (src/validate/__init__.py, matching the existing packages' __init__.py convention). Implement imagej_validation.py as a PURE module — no torch, no cv2, no sam2, no SAM2/image I/O; it takes already-measured px numbers + a px_per_cm and produces the comparison and the Markdown. Mirror the exact mm formula from build_final_csv (px / px_per_cm * 10) so the report is consistent with the real pipeline. Compute stdev_overshoot as: among threads with a non-None ImageJ stdev, the measured stdev_mm exceeds ImageJ stdev_mm for all (or a strict majority) of them. render_validation_md must include the Pitfall-2 explanation of StDev overshoot and the D2-03 statement that the fix (true perpendicular ray-cast, MEAS-04) is deferred to v2, not applied here. Unit-test with synthetic numbers only.
  </action>
  <verify>
    <automated>cd /home/bcjamal/projects/thread-compaction-analysis-auto && .venv/bin/python -m pytest tests/test_imagej_validation.py -q -m "not slow"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: given measured_px {5.11:{avg_diameter_px:129.3, stdev_px:23.7}} + px_per_cm chosen so measured avg mm ≈ 0.81, compare_to_imagej reports avg_pct_diff within ~5% of ImageJ's 0.789 and marks stdev_overshoot true (23.7px-derived stdev mm > ImageJ 0.111mm).
    - Behavior: a thread with ImageJ stdev None (5.22) does not raise and yields stdev_pct_diff None.
    - Behavior: render_validation_md output contains all provided thread ids, a "% diff" column, the Pitfall-2 StDev-overshoot note, and a MEAS-04 deferral pointer.
    - Source assertion: `grep -nE "import torch|import cv2|from sam2|Image.open" src/validate/imagej_validation.py` returns no matches (pure module).
  </acceptance_criteria>
  <done>compare_to_imagej + render_validation_md produce a correct per-thread mm comparison, handle the missing-stdev case, surface the Pitfall-2 overshoot + MEAS-04 pointer, and are import-pure; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Real-data validation harness + VALIDATION.md (all 4 ground-truth threads)</name>
  <files>scripts/run_imagej_validation.py, tests/test_imagej_validation.py, .planning/phases/02-batch-hardening-validation/VALIDATION.md</files>
  <read_first>
    - tests/test_segment_smoke.py (the established 5.11 click point (2740,1534) + its inspection rationale — reuse for 5.11, replicate the approach for the other threads)
    - src/segment/sam2_session.py (load_predictor/predict_mask — real CPU inference path)
    - src/measure/measure_masks.py (measure_mask — reused unchanged to get avg_diameter_px/stdev_px)
    - src/calibrate/ruler_scale.py (px_per_cm — used to derive the 08-03-25 factor from ruler ticks; DEFAULT_KNOWN_CM_SPAN=0.5, D-10)
    - tests/conftest.py (NEXTCLOUD_ROOT, ground_truth_imagej fixture, slow-marker convention)
    - src/validate/imagej_validation.py (Task 1 — compare_to_imagej/render_validation_md)
  </read_first>
  <action>
    Add scripts/run_imagej_validation.py exposing run_imagej_validation(nextcloud_root, out_md_path) that, for the real 08-03-25 folder (the D2-06 ground-truth validation folder — 08-04-25 has no ground truth and is not used for accuracy here): (1) derives the session px_per_cm — open 08-03-25/ruler.JPG, identify two points 0.5cm apart on the fine mm ticks (macro span per D-10), and compute the factor via calibrate.ruler_scale.px_per_cm; record the two source points and the resulting factor as a documented module constant PX_PER_CM_08_03_25 with an inline comment stating how the points were chosen (mirroring test_segment_smoke.py's inspection-rationale comment style). Since the Read tool downsamples large photos, prefer determining the ruler tick pitch by a short image-analysis pass (intensity peaks along a scan row across the fine ticks) over eyeballing, and note the method in the comment. (2) For each ground-truth thread, run real SAM2 CPU inference via predict_mask with a per-thread positive click point: reuse (2740,1534) for 5.11; determine 5.12/5.21/5.22 click points by inspecting each 08-03-25/<thread>.JPG (thread body center, away from the needle) and document each point + rationale in-code. (3) Run measure_mask on each mask (reused, unmodified) to get avg_diameter_px/stdev_px, assemble measured_px, call compare_to_imagej with the ground_truth mm values + PX_PER_CM_08_03_25, and write render_validation_md output to .planning/phases/02-batch-hardening-validation/VALIDATION.md. Then add a slow+integration test in tests/test_imagej_validation.py that calls run_imagej_validation, asserts LOOSE plausibility per D2-02 (not a strict gate) and that VALIDATION.md is produced with all 4 threads and the overshoot finding. Do NOT edit measure_masks.py. Run the harness once during the task to generate the committed VALIDATION.md.
  </action>
  <verify>
    <automated>cd /home/bcjamal/projects/thread-compaction-analysis-auto && git diff --exit-code src/measure/measure_masks.py && .venv/bin/python -m pytest tests/test_imagej_validation.py -q -m slow && test -f .planning/phases/02-batch-hardening-validation/VALIDATION.md && grep -q "5.22" .planning/phases/02-batch-hardening-validation/VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Behavior (slow, real SAM2): run_imagej_validation produces a report covering exactly the 4 threads 5.11/5.12/5.21/5.22; each measured avg_diameter_mm is finite and > 0.
    - Behavior (loose plausibility per D2-02, NOT strict): each thread's measured avg_diameter_mm lands within a documented loose band of its ImageJ value (e.g. |pct diff| < 25%) OR the divergence is explicitly recorded as a finding in VALIDATION.md — the test must not hard-gate on tight accuracy (only 4 ground-truth rows exist).
    - Artifact: .planning/phases/02-batch-hardening-validation/VALIDATION.md exists, lists all 4 threads with measured-vs-ImageJ + % diff, and contains the StDev-overshoot / MEAS-04-deferred finding.
    - Source assertion: `git diff --exit-code src/measure/measure_masks.py` returns clean (D2-03: measurement algorithm untouched); `grep -nE "2740|PX_PER_CM_08_03_25" scripts/run_imagej_validation.py` shows the documented click point + derived calibration constant.
  </acceptance_criteria>
  <done>The harness runs real SAM2 inference on all 4 ground-truth threads, compares measured mm to ImageJ, and emits VALIDATION.md with per-thread % diff + the Pitfall-2/MEAS-04 finding; measure_masks.py is unchanged; slow test green (or cleanly skipped if real data is absent, per conftest).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nextcloud source photos → validation harness | Read-only real 08-03-25 images (thread + ruler); harness must never write back into the source tree (EXPT-02) |
| validation harness → VALIDATION.md | New write target under .planning/ (report only, no pipeline data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | reads real source photos for inference | mitigate | harness only opens source photos read-only (np.array(Image.open(...))), writes nothing to the Nextcloud tree — all output goes to .planning/ VALIDATION.md |
| T-02-03-02 | Spoofing | hardcoded per-thread click points / calibration constant | accept | click points + PX_PER_CM_08_03_25 are documented in-code with derivation rationale; D2-02 makes the report a plausibility check, not a trusted gate |
| T-02-03-03 | Info disclosure | VALIDATION.md contains measured values + source paths | accept | local single-user research artifact; provenance is the point of the report |
| T-02-03-SC | Tampering | package installs | accept | no package installs this phase — SAM2/scikit-image already present from Phase 1 |
</threat_model>

<verification>
- Unit layer: `.venv/bin/python -m pytest tests/test_imagej_validation.py -q -m "not slow"` green.
- Real-data layer: `.venv/bin/python -m pytest tests/test_imagej_validation.py -q -m slow` green (or cleanly skipped if 08-03-25 absent).
- `git diff --exit-code src/measure/measure_masks.py` clean (D2-03).
- VALIDATION.md exists with all 4 threads + the StDev-overshoot / MEAS-04 finding.
</verification>

<success_criteria>
- All 4 ImageJ ground-truth threads measured and compared systematically (MEAS-03), replacing Phase 1's single spot-check.
- VALIDATION.md reports measured vs ImageJ + per-thread % diff (D2-02 plausibility report, not a pass/fail gate).
- The known StDev overshoot is documented as the Pitfall-2 boundary-roughness signature with a MEAS-04 (v2) pointer — no measurement-algorithm change (D2-03).
</success_criteria>

<artifacts_this_phase_produces>
- `src/validate/imagej_validation.py` — pure compare + report render (new)
- `src/validate/__init__.py` — package marker (new)
- `scripts/run_imagej_validation.py` — real-SAM2 validation harness + documented click points/calibration (new)
- `tests/test_imagej_validation.py` — unit + slow real-inference tests (new)
- `.planning/phases/02-batch-hardening-validation/VALIDATION.md` — the MEAS-03 validation report (new, committed)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-batch-hardening-validation/02-03-SUMMARY.md` when done.
</output>
